import { Fixture } from "@ethereum-waffle/provider";
import { Wallet } from "@ethersproject/wallet";
import { ethers } from "hardhat";


const poolFixture: Fixture = async (wallets, provider) => {
    // Create a pool
    ethers.getContractFactory("UniswapV3Factory")

    
}